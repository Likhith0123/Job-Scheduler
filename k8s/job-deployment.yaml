apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-service
  namespace: chronoflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: job-service
  template:
    metadata:
      labels:
        app: job-service
    spec:
      containers:
        - name: job-service
          image: chronoflow/job-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: chronoflow-config
            - secretRef:
                name: chronoflow-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: job-service
  namespace: chronoflow
spec:
  selector:
    app: job-service
  ports:
    - port: 8080
      targetPort: 8080
