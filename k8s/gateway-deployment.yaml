apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: chronoflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
    spec:
      containers:
        - name: gateway-service
          image: chronoflow/gateway-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: chronoflow-config
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: chronoflow
spec:
  selector:
    app: gateway-service
  ports:
    - port: 8080
      targetPort: 8080
